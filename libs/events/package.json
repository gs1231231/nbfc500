{
  "name": "@bankos/events",
  "version": "0.1.0",
  "description": "BankOS domain event name constants",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
