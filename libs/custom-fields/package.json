{
  "name": "@bankos/custom-fields",
  "version": "0.1.0",
  "description": "BankOS custom fields engine — tenant-defined fields on any entity",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
